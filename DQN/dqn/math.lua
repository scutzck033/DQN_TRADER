local dt=0.05
points=10
 s_index=1

function getSinValue(s_index, dt)
  return math.sin(s_index*dt)+1
  end

game_action={1,2,3}
game_action=game_action+1
--a=Step(0)
print (game_action)
print("-----------------------------")
print(reward)

